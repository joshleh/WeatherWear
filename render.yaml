services:
  - type: web
    name: weatherwear
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && pip install -e .
    startCommand: uvicorn weatherwear.webapp:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
